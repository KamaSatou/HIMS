Require to startup the system: 
- Set java to java 17
- Using maven to build the system.
- Create a database name hospitaldb first.
- Get the account password in PassGen.java using mvn command, then upload into the table users in the database to access full system.
Startup the backend with mvn command, frontend with index.html
